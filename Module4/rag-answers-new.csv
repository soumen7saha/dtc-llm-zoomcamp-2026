question,answer_llm,answer_orig,document
Is it okay to join the course late if I just found it now?,"Yes, you can still join the course late. If you want a certificate, make sure to submit your project while submissions are still open.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
Can I still take this course even if I missed the start date?,"Yes, you can still join even if you missed the start date. You can start whenever you want, as long as the course materials are available.

If your goal is to get a certificate, make sure to submit your project while submissions are still being accepted.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
"If I join after the course has already started, am I still eligible for a certificate?","Yes — you can still join after the course has started. If you want a certificate, you must submit your project while the course is still accepting submissions, and complete the required peer reviews during a live cohort.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
Do I need to submit my project before submissions close to get the certificate?,"Yes. To get the certificate, you need to submit your capstone project while submissions are still open and complete the required peer reviews.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
I’m a bit late to the course—what do I need to do to still earn the certificate?,"To still earn the certificate, you need to:

- **Finish a capstone project**
- **Complete the required peer reviews**
- **Submit both while a live cohort is still accepting submissions**

Homework is **not required** for the certificate. You can also catch up on the material in self-paced mode, but the **project submission and peer reviews must happen during the live cohort**.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
I registered for the LLM Zoomcamp — when should I expect a confirmation email?,"You don’t need to wait for a confirmation email — you’re accepted. Registration is only to gauge interest, and it isn’t checked against a registered list. You can start learning and submitting homework while the form is open.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
Do I actually need an acceptance email before I can start the course and hand in homework?,"No—you don’t need an acceptance email. You’re accepted, and you can start learning and submitting homework while the form is open. It isn’t checked against a registered list.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
"If I filled out the registration form, does that mean I’m officially on a checked list for the course?","No. The registration form is only to gauge interest, and it is not checked against any registered list. You can start learning and submitting homework while the form is open.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
"Is registering for the LLM Zoomcamp required, or is it just for estimating interest before the course starts?","Registering is just to gauge interest before the course starts. You don’t need to wait for a confirmation email, and registration is not checked against any accepted list. You can just start learning and submitting homework while the form is open.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
Can I begin learning and submit homework even if I never got any email after signing up?,"Yes. You don’t need the confirmation email — you’re accepted and can start learning and submit homework as long as the submission form is open, even if you never received any email after signing up.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
How do I join the Office Hours or live workshop if I don’t have the Zoom link?,"Students don’t use the Zoom link directly. For Office Hours or live/workshop sessions:

- Join via **YouTube Live** on the **DataTalksClub YouTube channel**
- Submit questions in **Slido** (the link is pinned in chat when live)
- Check the **announcements channel on Telegram and Slack** for the video URL before the session starts

The Zoom link itself is only published to instructors/presenters/TAs.","The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
"Where can I watch the live sessions for the course, and how do I ask questions during them?","You can watch the live sessions on the DataTalksClub YouTube channel, and the video link is also posted in the announcements channel on Telegram and Slack before the session starts.

To ask questions during the live sessions:
- Use Slido — the link is pinned in the live chat.
- Don’t post questions in the chat, since they may be missed.
- If you’re stuck outside the live session, ask in Slack and follow the asking questions guidelines.","The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
"Is the Zoom link for the workshop shared with students, or only with the teaching team?","The Zoom link is only published to the instructors/presenters/TAs, not to students.

Students join via YouTube Live, with the link posted in the announcements channel on Telegram and Slack before the session starts.","The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
What’s the best way to follow the live Office Hours stream if I’m a student?,"If you’re a student, the best way is to watch the stream on the DataTalksClub YouTube channel and follow the link posted in the announcements channel on Telegram or Slack before it starts. When the session is live, submit questions through Slido, not in chat.","The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
"Should I type my questions in the live chat during Office Hours, or use something else?",No — don’t post questions in the live chat. Use Slido instead; the Slido link is pinned in the chat when the session is live.,"The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
How do I find my row on the leaderboard if I can’t see my name right away?,"Click the **“Jump to your record on the leaderboard”** link — it will take you directly to your row, even if your name isn’t visible right away.","When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
Why does the leaderboard show some random nickname instead of my real name?,"The leaderboard uses your separate **display name**, not the real name you enter for the certificate.

By default, that display name is a **random nickname** like “Lucid Elbakyan,” so your real name won’t appear publicly. You can change it in **Edit Course Profile**:

- **First field:** public nickname for the leaderboard
- **Second field:** official name for your certificate

So if you’re seeing a random nickname, that’s normal and it’s there to keep your real name private.","When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
Where can I check what display name I’m using for the course?,Click **“Edit Course Profile”**. The **first field** is your **display name / nickname** used on the leaderboard.,"When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
"Can I change the name that appears publicly, like to my Slack or GitHub name?","Yes. You can change the public name in **Edit Course Profile** — the **first field** is your public nickname/display name, and you can set it to your Slack username, GitHub username, or any nickname you want.","When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
What should I put in the profile fields so my certificate doesn’t use the default random name?,"Put:

- **First field:** your **display/nickname** for the leaderboard
- **Second field:** your **official name** exactly as you want it printed on the certificate

If you leave the second field as the random default, your certificate will use that random name instead.","When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
Can I do the course at my own pace and still get a certificate at the end?,"No. You can work through the material in self-paced mode, but you only get a certificate if you finish with a live cohort and submit the capstone project plus the required peer reviews while submissions are still being accepted.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

To get the certificate, you need to finish a capstone project and complete the
required peer reviews. Homework is not required. You can work through the
material and prepare your project in self-paced mode, but project submission and
peer review must happen while a live cohort is accepting them.",69d122f12e
"If I take the self-paced version, is there any way to earn the course certificate?","No. You can only earn the course certificate if you finish the course with a live cohort. You may still work through the material and prepare your project in self-paced mode, but the project submission and required peer reviews must happen while a live cohort is accepting them.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

To get the certificate, you need to finish a capstone project and complete the
required peer reviews. Homework is not required. You can work through the
material and prepare your project in self-paced mode, but project submission and
peer review must happen while a live cohort is accepting them.",69d122f12e
Do certificates only count for people who finish with the live cohort?,"No. Certificates are only available if you finish the course with a live cohort. You can study in self-paced mode, but to get the certificate you must submit the capstone project and complete the required peer reviews while a live cohort is accepting them.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

To get the certificate, you need to finish a capstone project and complete the
required peer reviews. Homework is not required. You can work through the
material and prepare your project in self-paced mode, but project submission and
peer review must happen while a live cohort is accepting them.",69d122f12e
Why doesn’t the self-paced mode come with a certificate?,"You can only get a certificate if you finish the course with a live cohort. In self-paced mode, you can still work through the material and prepare your capstone project, but the certificate requires submitting the project and completing the required peer reviews while a live cohort is accepting them.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

To get the certificate, you need to finish a capstone project and complete the
required peer reviews. Homework is not required. You can work through the
material and prepare your project in self-paced mode, but project submission and
peer review must happen while a live cohort is accepting them.",69d122f12e
Is peer-review of the capstone project required in order to receive the certificate?,"Yes. To receive the certificate, you need to finish a capstone project and complete the required peer reviews.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

To get the certificate, you need to finish a capstone project and complete the
required peer reviews. Homework is not required. You can work through the
material and prepare your project in self-paced mode, but project submission and
peer review must happen while a live cohort is accepting them.",69d122f12e
Can I still get the certificate if I skipped the first homework?,"Yes. Homework is not mandatory, so skipping the first homework won’t prevent you from getting the certificate. You just need to pass the Capstone project and complete the required peer reviews while a live cohort is accepting them.","Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
Do I have to do all the homework to receive the certificate for this course?,"No. Homework is not required to get the certificate.

To receive the certificate, you need to:
- finish the capstone project, and
- complete the required peer reviews,

while the course is running with a live cohort.","Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
What do I actually need to pass in order to get the certificate?,"You need to **finish a capstone project** and **complete the required peer reviews** to get the certificate. Homework is **not required**. Also, you can only get the certificate if you finish the course with a **live cohort**.","Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
"Is homework mandatory, or is it just optional practice for the course?","Homework is **not mandatory**. It’s recommended for practice and the points count toward the leaderboard, but you can still get a certificate by passing the **capstone project**.","Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
"If homework isn't required, does it still affect anything like my ranking or points?","Yes. Homework isn’t required for the certificate, but its points do count toward your leaderboard rank.","Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
Why does the homework content keep changing in this course?,"Because the homework may still be in draft form. If the homework title contains `[DRAFT]`, it means it’s not ready yet, and the content can still change until:

- the homework form is open on the course management platform, and
- the title no longer contains `[DRAFT]`.

Working on it early is at your own risk, since the final version may differ.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
"Is the material already finalized, or can it still change later?","If the homework title contains `[DRAFT]`, it is not finalized yet, and the content can still change. It’s only ready when the form is open **and** the title does **not** contain `[DRAFT]`.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
"If I start working on the homework now, could the final version be different?","Yes — if the homework is still in `[DRAFT]` status or the homework form isn’t open yet, the final version can still change. Working on it early is at your own risk.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
How will I know when a module is actually ready and settled?,"The module is ready only when both are true:

- the homework form is open on the course management platform, and
- the homework title does **not** contain `[DRAFT]`.

Until then, it can still change.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
"Is it safe to do the course work in advance, or should I wait until updates are announced?","You can start whenever you want, but **working on the material or homework in advance is at your own risk** because the final version can still change until the homework is officially ready.

The homework is ready only when:
- the homework form is open on the course platform, and
- the homework title does **not** contain `[DRAFT]`.

So if you want to avoid mismatches with updates, it’s safer to wait until it’s announced as ready.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
When is the next time this course will be offered?,Summer 2027.,Summer 2027.,bd31146b0e
Do you know when the next course session starts?,The next course session starts in **Summer 2027**.,Summer 2027.,bd31146b0e
"Will there be another run of this course, and if so, when?",Yes — the course will be offered next in **Summer 2027**.,Summer 2027.,bd31146b0e
What's the next scheduled offering for this course?,Summer 2027.,Summer 2027.,bd31146b0e
Can you tell me when the course is coming back?,I don't know.,Summer 2027.,bd31146b0e
Where can I find the course lectures or videos for this class?,"Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders there — each lesson page has the relevant videos linked at the top. If you're unsure, follow the GitHub repo first, since it’s easier to keep updated than the YouTube playlist.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
Are the recordings or lecture videos posted somewhere I can watch them?,"Yes. Use the **LLM Zoomcamp GitHub repository** as the main entry point: https://github.com/DataTalksClub/llm-zoomcamp

Open the lesson folders there — each lesson page has the relevant videos linked at the top.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
I’m looking for the videos—should I check the bookmarks or pinned links?,"Check the **GitHub repo / lesson pages first** — that’s the main entry point, and each lesson page has the relevant videos linked at the top. If you’re unsure, the repo is better to follow than bookmarks or pinned links because it stays more up to date than the YouTube playlist.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
Is there a specific YouTube channel for the course lectures?,"Yes — the course lectures/videos are linked from the **LLM Zoomcamp GitHub repository**. Each lesson page has the relevant videos at the top, and the repo is the main entry point.

There isn’t a specific YouTube channel just for lectures mentioned in the context. If you need live sessions or office hours, those can also be watched on the **DataTalksClub YouTube channel**.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
"Where are the lecture videos linked, and what should I check first?","The lecture videos are linked at the top of each lesson page in the **LLM Zoomcamp GitHub repository**.  

What to check first: **the GitHub repo first**, since it’s the main entry point and is easier to keep updated than the YouTube playlist.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
Why does WSL2 say the model needs more memory even though my PC has enough RAM?,"Your WSL2 is not using all of your PC’s RAM. It only has access to the amount configured for WSL2, which is why it says the model needs more memory than is available.

To fix it, increase WSL2’s memory limit by creating a `.wslconfig` file in:

`C:\Users\YourUsername\.wslconfig`

with:

```ini
[wsl2]
memory=8GB
```

Then restart WSL:

```bash
wsl --shutdown
```

You can verify the new limit in WSL with:

```bash
free
```

","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
How can I increase the RAM available to WSL2 on Windows for running a model?,"Your WSL2 may be limited to only part of your computer’s RAM. To give it more memory:

1. Create a file called `.wslconfig` in your Windows user profile folder, for example:
   `C:\Users\YourUsername\.wslconfig`

2. Add this content:
   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL:
   ```bash
   wsl --shutdown
   ```

4. Open WSL again and run `free` to verify the available RAM.

","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
Where should I put a .wslconfig file to give WSL2 more memory?,"Put the `.wslconfig` file in your Windows user profile directory:

`C:\Users\YourUsername\.wslconfig`

","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
What do I need to write in .wslconfig to set WSL2 memory to 8GB?,"Write this in `.wslconfig`:

```ini
[wsl2]
memory=8GB
```

Then restart WSL with:

```bash
wsl --shutdown
```","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
"After changing WSL2 memory settings, how can I check if the new RAM limit is actually applied?",Run `free` inside WSL after restarting it with `wsl --shutdown`. That will let you verify whether the new RAM limit is applied.,"Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
"Why aren't we using LangChain in this course, and what should we learn first instead?","We’re not using LangChain because the course wants you to first build the core pieces yourself: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before moving on to a framework like React or Angular.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
Is there a reason the course skips LangChain for now?,"Yes — the course skips LangChain at the start on purpose.

The idea is to first build the core RAG pieces yourself: prompting, retrieval, indexing, and evaluation. LangChain is a framework that automates parts of this, but it’s easier to use frameworks well once you understand what they’re doing under the hood.

A good analogy is learning HTML, CSS, and JavaScript before moving to React or Angular.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
Why start without a framework like LangChain when building LLM apps?,"Because the course wants you to learn the core LLM app building blocks first: prompting, retrieval, indexing, and evaluation.

LangChain is a framework for building LLM-powered apps, but it automates many of those pieces. Starting without it is like learning HTML, CSS, and JavaScript before using React or Angular. Once you understand the fundamentals, frameworks are much easier to use well.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
"Should I learn the basics before using LangChain, and why?","Yes — it’s better to learn the basics first, because LangChain is a framework that automates parts of building LLM apps. The course recommends first understanding the core pieces yourself: prompting, retrieval, indexing, and evaluation.

A good analogy is learning HTML, CSS, and JavaScript before React or Angular. Once you know the fundamentals, you can use the framework much more effectively.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
What’s the idea behind not using LangChain in the course material?,"The idea is to first learn the core building blocks yourself: prompting, retrieval, indexing, and evaluation. LangChain is a framework for LLM apps, but the course wants you to understand what it automates before using it.

It’s like learning HTML, CSS, and JavaScript before moving on to React or Angular.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
Why do I get an OpenAI API error about insufficient credits when I run `chat.completions.create`?,"Because your OpenAI account doesn’t have enough API quota/credits. The context says this can happen with OpenAI API calls like `responses.create`, and the same quota issue can also affect `chat.completions.create`.

Typical fix:
1. Add credits / enable pay-as-you-go in your OpenAI billing settings.
2. Make sure you’re using a model available to your account.
3. If needed, recreate your API key after adding credits.

If you’re using a non-OpenAI provider through an OpenAI-compatible endpoint, this may also be a quota/endpoint mismatch issue.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
"What does the OpenAI ""Insufficient credits"" message mean in `chat.completions.create`?","It means your OpenAI account doesn’t have enough API credits left to complete the request.

","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
I’m seeing an OpenAI API error about credits when testing chat completions — what should I check?,"Check your OpenAI API account quota/billing. This error usually means you have **insufficient credits** or **hit your current quota**.

What to do:
1. Add credits / enable pay-as-you-go in your OpenAI billing settings.
2. Make sure you’re using a model available to your account in `responses.create(...)`.
3. If needed, regenerate your API key after adding credits and update it locally.

If you’re still seeing the issue after that, I don’t know.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
Is the `OpenAI API Error: Insufficient credits` issue caused by my `chat.completions.create` call?,"No. Based on the context, `OpenAI API Error: Insufficient credits` is caused by insufficient quota/credits in your OpenAI account, not by the `chat.completions.create` call itself.

You need to add billing credits and possibly recreate your API key.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
What’s the reason for the credit-related error when using OpenAI chat completions?,"The error is due to **insufficient quota/credits in your OpenAI API account**. The context says there is **no free usage** of the OpenAI API, so you need to **add billing credits** (at least $5), and possibly **recreate your API key** after adding funds.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
Why am I getting a 429 RateLimitError from OpenAI even though my code looks fine?,"You’re most likely hitting an **API quota/billing limit**, not a bug in your code.

A `429 RateLimitError` from OpenAI in this context can mean:

- **You exceeded your current quota**
- Your account has **no billing credits / no pay-as-you-go enabled**
- Your API key may need to be **recreated after adding credits**

What to do:

1. **Check billing/credits** in your OpenAI account.
2. **Add funds** if you’re on a free/low-spend account.
3. Make sure you’re using a **model available to your account**.
4. If needed, **create a new API key** and update it locally.

If the error message says `insufficient_quota`, that confirms it’s a **billing/quota issue**, not a code issue.","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
"Does OpenAI API have any free quota for this kind of request, or do I need to pay first?","No — there is no free usage quota for the OpenAI API. You need to add funds and pay first, even for this kind of request.

If you want, you can use a free or low-cost alternative instead, as listed in the course repo.","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
I added billing but still get an insufficient_quota error — do I need to make a new API key?,"You may need to recreate the API key after adding billing, and then update it locally.","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
Which model should I use in chat.completions.create if my current one is not available to my account?,"Use the **chat-completions** API with a model that is **currently available to your account**.

For non-OpenAI providers, the course says to do:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
)
return response.choices[0].message.content
```

If your current model is unavailable, replace it with another model id that your provider/account supports.","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
What are the exact steps to fix the OpenAI quota error and get my request working again?,"To fix the OpenAI quota error and get your request working again, follow these steps:

1. Add credits to your OpenAI account here:  
   https://platform.openai.com/settings/organization/billing/overview  
   Minimum suggested amount: $5

2. Make sure your code uses a model that is available to your account in `responses.create(model='gpt-4o', …)`.

3. If needed, recreate your API key after adding credits and update it locally.

4. Re-run your Python command.

If you still get `insufficient_quota`, that’s a billing issue, not a rate-limit issue.","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
How much do I need to spend to start using the OpenAI API?,"You can start with a very small amount: the course notes say you can recharge starting from **$5**, and typical initial usage is usually **fractions of a cent**.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
Is it expensive to use the OpenAI API for this course?,"No — you don’t have to pay for OpenAI API to complete the course homework. The course says you can use free or low-cost alternatives, and if you do use OpenAI API, it should cost very little — initial usage is usually fractions of a cent, and you can recharge starting from $5.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
Do I have to put in a lot of money before I can try the OpenAI API?,"No. You can start with a very small amount — OpenAI lets you recharge starting from **$5**, and for the course the initial usage is usually **fractions of a cent**. Also, you don’t have to use OpenAI at all; free or low-cost alternatives are fine.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
What is the minimum top-up amount for the OpenAI API?,The minimum top-up amount for the OpenAI API is **$5**.,"Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
Can I get started with OpenAI API usage for just a few cents?,"Yes. According to the course context, using the OpenAI API for the course should cost very little — you can recharge starting from $5, and initial usage is usually fractions of a cent.

If you want, you can also use free or low-cost alternatives instead of OpenAI.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
"Do I need to pay for the OpenAI API to finish the homework in this course, or are there free options I can use instead?","No, you don’t have to pay for the OpenAI API to complete the homework.

The course says you can use free or low-cost alternatives instead, and you’re allowed to use other LLM providers or even locally hosted models. The course repo has a list of OpenAI API alternatives.","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
"For the LLM Zoomcamp assignments, is a paid OpenAI subscription required, or can I rely on the free alternatives from the course repo?","No, a paid OpenAI subscription is **not required** for the LLM Zoomcamp homework. You can use the **free or low-cost alternatives** listed in the course repo’s OpenAI API alternatives section.

If you want, I can also point you to the kinds of alternatives the course mentions.","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
"Can I complete the course tasks without buying OpenAI API access, and where are the no-cost alternatives listed?","Yes — you do **not** have to buy OpenAI API access to complete the course homeworks. You can use **free or low-cost alternatives** instead.

The no-cost alternatives are listed in the course GitHub repo here:

https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
"Is paying for OpenAI necessary for the homeworks, or does the GitHub have some free API alternatives I can use?","No, you don’t have to pay for OpenAI to do the homeworks. The course explicitly says you can use free or low-cost alternatives from the GitHub repo.

You can check the list here:
https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives

Also, the course is not tied to OpenAI — you can use other providers like Groq, OpenRouter, Gemini, Mistral, or even run models locally with Ollama, vLLM, or LM Studio.","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
"To do the course exercises, do I have to subscribe to OpenAI, or is there a list of free replacements somewhere in the course materials?","No, you don’t have to subscribe to OpenAI to do the exercises. The course materials include free or low-cost alternatives, and there’s a curated list in the repo:

- [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives)

You can also use other providers or even local models instead of OpenAI.","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
How do I keep my API key in a .env file and read it in Python?,"Store it in a `.env` file, add that file to `.gitignore`, and load it in Python with `python-dotenv`.

Example:

```gitignore
.env
```

```bash
OPENAI_API_KEY=sk-...
```

Install:

```bash
pip install python-dotenv
```

Read it in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

api_key = os.getenv(""OPENAI_API_KEY"")
```

Make sure the variable name in `.env` matches exactly.","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
What imports do I need to load environment variables from a .env file?,"You need:

```python
from dotenv import load_dotenv
import os
```

Then call:

```python
load_dotenv()
```

And read variables with:

```python
os.getenv(""OPENAI_API_KEY"")
```","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
How can I point Python to the right .env path and get the key value?,"Use `python-dotenv` and make sure Python loads the `.env` file before you read the variable.

```bash
pip install python-dotenv
```

```python
import os
from dotenv import load_dotenv

load_dotenv()  # loads .env from the current working directory

key = os.getenv(""OPENAI_API_KEY"")
print(key)
```

If your `.env` file is in a different location, point to it explicitly:

```python
load_dotenv(dotenv_path=""/path/to/your/.env"")
```

Then get the value with `os.getenv(""OPENAI_API_KEY"")`.

If it still doesn’t work, I don't know.","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
What's the easiest way to access API_KEY_abc after loading .env?,"Use `os.getenv` after `load_dotenv()`:

```python
import os
from dotenv import load_dotenv

load_dotenv()

api_key = os.getenv(""API_KEY_abc"")
```

If you want, I can also show the one-liner version.","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
"Do I need to add my .env file to .gitignore, and why?","Yes. Add your `.env` file to `.gitignore` so your API keys are never committed to GitHub.

Why:
- `.env` often contains secrets like `OPENAI_API_KEY`, `GROQ_API_KEY`, etc.
- Committing it would expose those credentials publicly.
- The course recommends keeping `.env` out of version control and using a `.env.example` file with only the variable names for reproducibility.

Example:
```gitignore
.env
```

For sharing your project, keep:
- `.env` = private, not committed
- `.env.example` = committed, with no secret values","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
"Why does my OPENAI_API_KEY keep showing as missing inside Jupyter, and how do I fix it?","Inside Jupyter, `OPENAI_API_KEY` usually shows as missing because the notebook hasn’t loaded your `.env` file into the Python session.

Fix it like this:

1. Install `python-dotenv`:
```bash
pip install python-dotenv
```

2. Load the `.env` file in your notebook **before** creating the OpenAI client:
```python
from dotenv import load_dotenv

load_dotenv()
```

3. Make sure the variable in your `.env` file is named exactly:
```env
OPENAI_API_KEY=your_key_here
```

If it’s still missing, double-check that the notebook is running in the same directory as the `.env` file, or that the path to the file is correct.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
"If I already put the key in a .envrc file, why is the notebook still throwing the api_key error?","Because `.envrc` is not automatically read by the notebook.

In the notebook, you need to load the environment file explicitly, for example with `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()
```

Also make sure the variable is named exactly `OPENAI_API_KEY`.

If you’re still seeing the error after that, I don’t know.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
What’s the easiest way to make OpenAI API auth work in a notebook when direnv isn’t enough?,"The easiest way is to load your API key from a `.env` file inside the notebook with `python-dotenv`:

```bash
pip install python-dotenv
```

```python
from dotenv import load_dotenv
load_dotenv()
```

Then make sure the variable is named exactly `OPENAI_API_KEY` in your `.env` file.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
How do I use python-dotenv to load my environment variables from .envrc in Jupyter?,"`python-dotenv` loads a `.env` file, not `.envrc`.

In Jupyter, do this:

```python
from dotenv import load_dotenv

load_dotenv()  # loads .env from the current project root
```

If your variables are in `.envrc`, the context doesn’t say `python-dotenv` loads that file directly. I don’t know.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
"Can I store OPENAI_API_KEY in GitHub Codespaces instead of setting it locally, and how do I do that?","Yes — for GitHub Codespaces, you should use **Codespaces secrets** instead of storing `OPENAI_API_KEY` in a local `.env` file in the repo.

The course notes say:

- **For GitHub Codespaces, use the built-in Codespaces secrets**
- This is the preferred way to keep the key available in your Codespace without putting it in the repository

How to do it:
1. Open your GitHub Codespaces settings for the account/repo.
2. Create a new secret named `OPENAI_API_KEY`.
3. Paste your API key as the value.
4. Restart or reopen the Codespace if needed so it becomes available.

If you want the key available automatically in a local setup instead, the course recommends using `dirdotenv` with a `.env` file — but for Codespaces specifically, use secrets.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
How can I figure out the GPT-4o token count for homework 6 if I’m using Groq and there’s no tokenizer for it?,"Use Groq’s response object and read the usage/token fields from it, rather than trying to tokenize GPT-4o text yourself.

If Groq exposes token usage, use those fields directly. If it doesn’t, use Groq’s native tokenizer as an approximation. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Groq/GPT-4o, because tokenization differs by model.

If your question is specifically about homework scoring, choose the closest option.","For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
Do I need an OpenAI API key just to check how many tokens a prompt has in this assignment?,I don't know.,"For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
What library should I use to estimate the number of OpenAI tokens in question 6?,Use `tiktoken`.,"For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
Is there a way to count GPT-4o tokens without using the OpenAI API itself?,I don't know.,"For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
"If Groq doesn’t have a tokenizer, what’s the simplest way to get the token number the homework wants?","The simplest way is to **use the token count returned by the model response object**.

For Groq, check the response’s `usage` fields if they’re available. If Groq doesn’t expose token usage in the response, then use Groq’s **native tokenizer** as an approximation.

Do **not** use `tiktoken` or `cl100k_base` as a generic tokenizer for Groq.","For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
Can I swap OpenAI for Groq in the course exercises and final project?,"Yes — you can swap OpenAI for Groq in the course exercises, as long as the code uses an OpenAI-compatible endpoint.

For the course code, that usually means changing:
- `base_url` to Groq’s OpenAI-compatible URL
- the API key
- the model name

For example, Groq works with the OpenAI client when pointed at:

`https://api.groq.com/openai/v1`

For the capstone/final project, you’re not restricted to OpenAI either — you can use Groq, another provider, or even a local model.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
Is it okay to use a different LLM platform instead of OpenAI for the homework?,"Yes — you can use a different LLM platform/provider instead of OpenAI. The course isn’t tied to OpenAI, and anything with an OpenAI-compatible endpoint or a locally served model should work.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
"Do I need a paid API like OpenAI to finish the assignments, or can I run everything locally?","No — you don’t need a paid OpenAI API to finish the assignments.

You can use:
- a different hosted provider,
- an OpenAI-compatible API,
- or run a model locally with tools like Ollama, vLLM, or LM Studio.

The homework is designed so you do not need a paid service. If you use a local model or another provider, you may need to adjust the code a bit.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
"If I use another model provider, will I have to change the code myself?","Yes — in many cases you’ll need to change the code a little.

If the other provider is **OpenAI-compatible**, you usually only need to update:
- `base_url`
- API key
- model name

But for some providers you may also need to adapt:
- **response format**
- **tool schema**
- **token counting / tokenizer logic**

So: **sometimes only small config changes, sometimes a bit of code adjustment**.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
Where should I look if I want to adapt the project to a platform like Groq?,"Look at the project examples from previous years to understand the expected scope:

- 2024 projects: https://courses.datatalks.club/llm-zoomcamp-2024/projects
- 2025 projects: https://courses.datatalks.club/llm-zoomcamp-2025/projects

If you want feedback on your adaptation idea, you can also ask in Slack.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
"Why does my homework query return an empty list after filtering, even though the data is there?","This is likely an indexing issue. You need to add the index settings **before** inserting the data into the indices; otherwise, filtering and querying may return an empty list even though the data exists.","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
What am I missing if my Q3 filter gives no results in the LLM Zoomcamp homework?,I don't know.,"This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
Could this be an indexing problem if the search comes back empty after I apply the filter?,"Yes — this is likely an indexing problem. If the search returns empty after applying a filter, the context says the first thing to check is whether the data was indexed correctly. In particular, make sure you add the index settings before inserting the data into the index.","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
Do I need to set up the index differently before loading the data for this assignment?,"Yes — you should set up the index settings **before** loading the data into the index. If the settings are added after the data, it can cause indexing problems and lead to empty results when filtering.","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
How should I fix the empty results issue in HW Q3 when using filters and queries?,"This is likely an indexing issue. You should **add the index settings before adding the data** to the index. Once the data is indexed correctly, your filters and query should work and stop returning an empty list.","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
Why do I get a different token count in tiktoken than what OpenAI shows in the API response?,"Because `tiktoken` only counts the tokens in the text you see, while OpenAI’s API also counts hidden formatting tokens added by the chat/completions format.

In particular:

- Each chat message adds about 4 structural tokens
- The API adds 2 extra tokens to mark the assistant response start
- Whitespace, newlines, and some Unicode characters can also change the count

So a prompt that looks like 320 tokens in `tiktoken` might show as 327 in OpenAI’s response.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
"How come my prompt has 320 tokens locally, but OpenAI says 327 for the same request?","Because OpenAI’s API counts more than just your visible text.

Even if `tiktoken.encode()` shows 320 tokens locally, OpenAI may report 327 because:

- each chat message adds structural tokens for the role/content/separators
- the API adds 2 tokens to mark the start of assistant generation
- extra whitespace, newlines, or unusual Unicode can add a few more

So the difference is due to OpenAI’s internal chat formatting, not necessarily an error in your local count.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
Does chat.completions.create add extra tokens beyond the text I send?,"Yes. `chat.completions.create()` can add extra internal tokens beyond the text you send.

According to the course context:
- Each message adds about **4 structural tokens** for chat formatting.
- The API also adds **2 tokens globally** to mark the assistant response start.
- Extra whitespace, newlines, or uncommon Unicode can add a few more.

So OpenAI’s reported token count can be higher than a plain `tiktoken.encode()` count of your visible text.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
What makes OpenAI count more tokens than just encoding the visible message content?,"OpenAI counts more tokens because chat requests include hidden formatting tokens, not just the visible text.

Specifically:
- each message adds 4 structural tokens for things like the role, separators, and content framing
- the API adds 2 global tokens to start assistant generation
- extra whitespace or unusual Unicode can also add a few tokens

So the reported count can be higher than the token count of the plain message content alone.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
"Can whitespace, newlines, or weird Unicode characters change the token total in OpenAI chat requests?","Yes. In OpenAI chat requests, extra whitespace, newlines, and some uncommon Unicode characters can slightly change the token total. The course context notes that OpenAI also adds internal chat-formatting tokens, so the count you get from `tiktoken.encode()` may differ from what OpenAI reports.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
How can I set my API key once so I don’t have to export it in every new terminal?,"Use `dirdotenv` so the key is loaded automatically when you enter the project folder.

```bash
uv tool install dirdotenv
```

Then add this to your `~/.bashrc` or `~/.zshrc`:

```bash
eval ""$(dirdotenv hook bash)""   # or zsh
```

Inside your project, create a `.env` file:

```bash
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the API key will be loaded automatically whenever you `cd` into that directory.

Also make sure `.env` and `.envrc` are in `.gitignore` so you never commit your key.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
Is there a way to make env vars load automatically when I enter a project folder?,"Yes — you can make them load automatically when you enter a project folder by using a tool like `direnv`.

The course context specifically recommends loading env vars with `python-dotenv` inside Python, but for automatic loading in the shell, `direnv` is a common option.

If you want, I can show you a simple setup for `direnv`.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
What’s the easiest setup for keeping my OpenAI key in a project without putting it in the code?,"The easiest setup is to put your key in a `.env` file and load it from there, instead of hardcoding it in the project.

For example:

```env
GEMINI_API_KEY=your-key-here
```

Then in code:

```python
import os
from dotenv import load_dotenv

load_dotenv()
api_key = os.getenv(""GEMINI_API_KEY"")
```

The context also notes that you should never commit your keys to Git.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
How should I handle API keys in GitHub Codespaces so they aren’t stored in the repo?,"Use GitHub Codespaces secrets instead of putting API keys in files in the repo.

If you’re working locally, store keys in a `.env` file and add `.env` to `.gitignore` so it never gets committed.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
"If I’m running Python scripts, what can I use to load a .env file automatically?","Use `python-dotenv` and call `load_dotenv()` in your script:

```python
from dotenv import load_dotenv

load_dotenv()
```

This automatically loads variables from the `.env` file.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
"How do I install Ollama on macOS, Windows, or Linux for this course?","Install Ollama from the official download page: https://ollama.com/download

- **macOS**: download the `.pkg` installer and run it.
- **Windows**: download the `.msi` installer and run it.
- **Linux**: run:
  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

After installation, you can test it with:

```bash
ollama run llama3
```

And optionally check the local server with:

```bash
curl http://localhost:11434
```","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
What should I run after installing Ollama to download and start LLaMA 3 locally?,"Run:

```bash
ollama run llama3
```

This will download and start LLaMA 3 locally.","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
How can I quickly check if the Ollama server is actually running on my machine?,"Quickly test it with:

```bash
curl http://localhost:11434
```

If Ollama is running, you should get a response like:

```json
{""models"": [...]}  
```

If you get a connection error, the server is not running.","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
"After installing Ollama, how do I get the Python package and use it from a script?","Install the Python client with:

```bash
pip install ollama
```

Then use it in your script like this:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```

Make sure Ollama is running locally first, for example by starting it with:

```bash
ollama run llama3
```","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
What’s the simplest example of calling Ollama chat from Python with a prompt?,"Here’s the simplest Python example from the course context:

```python
import ollama

your_prompt = ""What is RAG?""

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
"Why am I getting a connection refused error when I prompt my Ollama RAG, and how do I fix it?","You’re likely getting the error because the Ollama server is not running or was stopped when the notebook cell was restarted.

To fix it, restart Ollama with:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Also, if you stop and restart the notebook cell, make sure to rerun the cell that starts `ollama serve`.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
My homework throws connection refused with Ollama — do I need to restart the server somehow?,"Yes — if you get a **connection refused** error with Ollama during the homework, restart the Ollama server with:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

If you stop and restart the notebook cell, rerun the cell that contains `ollama serve` as well.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
What should I run again if Ollama stops working after I restart the notebook cell?,"Run the cell containing `ollama serve` again:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
Is there a command to bring Ollama back up when the RAG prompt fails with connection refused?,"Yes — restart the Ollama server with:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

If you stop and restart the notebook cell, rerun the cell that contains `ollama serve`.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
I closed or restarted the notebook and now Ollama won’t respond; what’s the quickest way to recover it?,"Run:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

If you restarted or stopped the notebook cell, rerun the cell that starts `ollama serve`.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
Which free Groq models support tool use right now?,"Several free Groq models support tool use right now. The context specifically mentions **Deepseek R1** and **Llama 4** as examples of Groq models that can be used for free for development.

I don't know the full current list beyond those examples.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Can I use Deepseek R1 with tools on Groq without paying?,I don't know.,"Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Does Llama 4 on Groq work for tool calling in dev?,"Yes. The context says several Groq models offer tool use, including **Llama 4**, and they can be used **for free for development**.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Are there any no-cost models I can test with tool use support?,"Yes. Several Groq models offer tool use and can be used for free for development, including Deepseek R1 and Llama 4.

Other providers also support tool/function calling, such as Mistral, Gemini, and some local Ollama models.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Where can I read the docs for Groq tool use and free models?,"You can read the Groq tool use docs here: [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).

For free models with tool use support, the context mentions Groq models like **Deepseek R1** and **Llama 4**, which can be used for free for development.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Why do I get `AttributeError: 'str' object has no attribute 'output'` when I call OpenAI Responses API with a non-OpenAI model?,"Because `client.responses.create(...)` and `response.output` are **OpenAI Responses API features**. Non-OpenAI providers like Groq, Gemini, Mistral, etc. don’t implement that API shape, so the SDK call ends up returning a plain string or a different object — and then `response.output` fails with:

`AttributeError: 'str' object has no attribute 'output'`

For non-OpenAI models, use the **chat completions** API instead:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # adapt to the provider if needed
)

return response.choices[0].message.content
```

If you need, I can also show how to adapt the tools schema for a specific provider.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
"Can I use `client.responses.create(...)` and `response.output` with Mistral, Groq, or Gemini in my agent code?","No. `client.responses.create(...)` and `response.output` are OpenAI-specific and are not implemented by Mistral, Groq, Gemini, etc.

For those providers, use the chat-completions API instead and read:

```python
response.choices[0].message.content
```

You may also need to adapt the tools schema to whatever format that provider expects.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
What should I use instead of the Responses API if I'm switching my LLM provider away from OpenAI?,"Use the **chat-completions API** instead of the Responses API.

For non-OpenAI providers, call:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,
)
print(response.choices[0].message.content)
```

Also, you may need to **adapt the tools schema** to match what your provider expects.

If your provider supports an OpenAI-compatible endpoint, you can often keep the OpenAI client and just change the `base_url`, API key, and model name.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
How do I read the assistant reply from chat completions when the provider doesn't support OpenAI's Responses API?,"Use the older chat-completions API and read the assistant text from:

```python
response.choices[0].message.content
```

Example:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # adapt per provider if needed
)

reply = response.choices[0].message.content
```

If you want, I can also show how to adapt the tools schema for a specific provider.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
Do I need to change my tools schema too when I move from OpenAI to another model provider?,"Yes — if you move from OpenAI to another provider, you may need to adapt the tool schema to whatever shape that provider expects.

The course notes say that response formats, tool schemas, and tokenizers can differ between providers. If you use a non-OpenAI provider, it’s common to switch to `chat.completions` and adjust the tools schema accordingly.

So the short answer is: **possibly yes**. If the provider is OpenAI-compatible, you may only need small changes; if not, you’ll likely need to adapt the schema.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
Why am I getting a 400 missing required parameter for tools[0].function in Azure OpenAI?,"You’re getting that Azure OpenAI 400 error because the tool schema is missing the required `function` object inside the tool definition.

Use this format:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```

In short: Azure OpenAI expects the tool to include `type: ""function""` and a nested `function` field with `name`, `description`, and `parameters`.","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
What should the get_weather_tool JSON look like to fix the tools[0].function error?,"```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
How do I define a weather function tool so Azure OpenAI accepts it?,"Use this `get_weather_tool` JSON shape:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```

This matches what Azure OpenAI expects for a function tool.","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
My Azure OpenAI request fails with missing tools[0].function — what am I doing wrong?,"You’re using the wrong tool schema for Azure OpenAI.

Azure expects each tool to include a nested `function` object. Use this format:

```python
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": False
        }
    }
}
```

So the issue is that your current tool definition is missing `tools[0].function`.","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
Can you show the correct function tool schema for get_weather in Module 2 agents?,"Yes — for Module 2 agents, the `get_weather` tool schema should be:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```

","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
"What do embeddings mean in vector search, and why do we convert text into numbers?","Embeddings are a way of converting text or other non-numerical data into numbers while preserving meaning and context. In vector search, similar texts should end up with similar vectors, so we can compare them mathematically.

We convert text into numbers because computers can’t directly compute semantic similarity on raw text. Once text is represented as vectors, we can use operations like dot product or cosine similarity to find the most similar items efficiently.","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
How can an embedding keep the meaning of words or documents when turning them into numerical values?,"An embedding keeps meaning by converting non-numerical data into numerical data **while preserving meaning and context**.  

In practice, similar words or documents are turned into **similar numerical vectors**, so their proximity in vector space reflects semantic similarity.","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
Why do similar inputs end up with close vectors in an embedding model?,"Similar inputs end up with close vectors because the embedding model is trained so that semantically similar texts are mapped to nearby vectors in vector space.

I don't know.","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
How are embeddings used to measure semantic similarity between pieces of text?,"Embeddings convert text into numerical vectors that preserve meaning and context. To measure semantic similarity, you compare these vectors: texts with similar meanings should have embeddings that are close together in vector space. This is often done with cosine similarity, which checks how aligned two embedding vectors are. If the embeddings are normalized, cosine similarity can be computed efficiently with a dot product.","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
"What is the connection between embeddings, vector space models, and dimensionality reduction?","Embeddings are one way of creating a **vector space model**: they turn non-numerical data into numerical vectors so that similar items end up close together in a vector space.

**Dimensionality reduction** is related because embeddings often represent data in a lower-dimensional vector form that still preserves meaning and context.

So the connection is:

- **Vector space model**: the general idea of representing data as vectors
- **Embeddings**: a modern way to build such vector representations
- **Dimensionality reduction**: the process of compressing data into fewer dimensions while preserving important structure

","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
How do I get rid of the warning about multi-qa-mpnet-base-dot-v1 and sentence-transformers bet?,"To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher:

```bash
pip install sentence-transformers>=3.0.0
```

","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
What should I install to suppress the model warning in vector search?,"Install `sentence-transformers>=3.0.0`:

```bash
pip install sentence-transformers>=3.0.0
```

","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
Is there a fix for the message saying this model was made with sentence-transformers v3?,"Yes. Upgrade `sentence-transformers` to version 3.0.0 or higher:

```bash
pip install sentence-transformers>=3.0.0
```","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
Which sentence-transformers version do I need so that the warning stops showing up?,"You need `sentence-transformers` version **3.0.0 or higher**.

Example:
```bash
pip install sentence-transformers>=3.0.0
```","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
How can I upgrade my setup to avoid that bet warning from the embedding model?,"Upgrade `sentence-transformers` to version 3.0.0 or higher:

```bash
pip install sentence-transformers>=3.0.0
```

This should suppress the warning.","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
"Why did the lesson use `.dot(...)` for cosine similarity, but the homework says to normalize first?","In the lesson, `.dot(...)` was used because the embeddings were assumed to already be **normalized to unit length**, so the dot product is the same as cosine similarity.

In the homework, the embeddings are **not normalized by default** (e.g. TF-IDF + SVD), so you need to **normalize first** if you want the dot product to represent cosine similarity.

So the difference is:

- **Lesson:** normalized embeddings → `.dot(...)` works as cosine similarity
- **Homework:** unnormalized embeddings → normalize first, then use dot product

","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
"Are the embeddings from `model.encode(...)` already unit-length, or do I need to normalize them myself?","Yes — in the lesson context, embeddings from `model.encode(...)` are assumed to already be normalized to unit length, so you do **not** need to normalize them yourself.

If you’re using embeddings like TF-IDF + SVD in the homework, those are **not** normalized by default, so you **do** need to normalize them manually if you want cosine similarity.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
When is dot product the same as cosine similarity in vector search?,"Dot product is the same as cosine similarity when the vectors are **normalized to unit length**.

In that case, their magnitudes are 1, so:

- **dot product = cosine similarity**

This is why in vector search you often see matrix multiplication used directly for cosine similarity: the embeddings and query are normalized first.

If the vectors are **not normalized**, then dot product is **not** the same as cosine similarity.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
Why doesn’t dot product work as cosine similarity for TF-IDF + SVD embeddings?,"Because TF-IDF + SVD embeddings are **not normalized by default**.

- For cosine similarity, you need vectors with unit length, or you need to divide by their norms yourself.
- A plain dot product only equals cosine similarity when both vectors are already normalized.
- In the lesson, `.dot(...)` worked because the model embeddings were assumed to be normalized.
- In the homework, TF-IDF + SVD vectors can have different magnitudes, so the dot product also reflects vector length, not just direction.

So for TF-IDF + SVD, **normalize first** if you want cosine similarity.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
"In the homework, what changes if I skip vector normalization before comparing documents?","If you skip vector normalization, the dot product no longer equals cosine similarity.  
That means document comparisons can be biased by vector length, so the similarity scores may change and the ranking can be wrong.

I don't know.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
"For vector search, do I index the question, the answer, or both together?","It depends on your dataset — there’s no single right answer.

The course recommends trying three options and measuring which works best on your ground-truth set:

- index the **answer only** (`text`)
- index the **question only**
- index **question + answer together**

Often, **question + answer** gives the best hit rate / MRR, but it also makes the input longer and slightly more expensive.

So the practical answer is: **experiment and pick the option that performs best on your data**.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
Is it better to embed just the answer text or the question text when building the vector index?,"There’s no single best choice — it depends on your dataset, so you should test it.

The course recommends trying these options and comparing hit rate / MRR on your ground-truth set:

- embed the **answer text** only
- embed the **question** only
- embed **question + answer text** together

Often, **question + answer text** works best, but it’s a bit longer and slightly more expensive.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
"Why would combining the question and answer into one embedding help, and is it worth the extra cost?","Combining the question and answer into one embedding can help because it gives the retriever more context, so the embedding may better capture the full meaning of the item. In practice, the course notes say this is often the best option.

Is it worth the extra cost? Usually, only a little extra cost, since the input is longer — but whether it’s worth it depends on your dataset. The recommendation is to test it and pick whichever gives the best hit rate / MRR on your ground-truth set.

","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
How do I decide which embedding setup gives the best retrieval results on my data?,"Run an evaluation on your ground-truth set and compare retrieval metrics like **hit rate** and **MRR**.

For the embedding setup specifically, the course suggests trying these options:

- embed the **answer** only
- embed the **question** only
- embed **question + "" "" + text**

There’s no universal best choice — the best setup is the one that gives the highest hit rate / MRR on **your** data.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
"Does the course recommend one embedding strategy for vector search, or is it something I need to test myself?","There’s no single recommended embedding strategy. The course says it’s something you should **test on your own dataset**.

It shows three options:
- embed the **answer** only
- embed the **question** only
- embed **question + text**

Then compare which one gives the best **hit rate / MRR** on your ground-truth set.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
How does cosine similarity measure how close two vectors are in text search?,"Cosine similarity measures how close two vectors are by looking at the **angle between them**, not their raw length. In text search, if two document vectors point in a similar direction, they’re considered more similar.

- **1** means the vectors are identical in direction
- **0** means they are unrelated/orthogonal
- **-1** means they point in opposite directions

When vectors are **normalized to unit length**, cosine similarity becomes the same as the **dot product**, which is why it can be computed efficiently with matrix multiplication.","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
Why is cosine similarity used for comparing documents instead of just their raw word counts?,"Because raw word counts are affected by document length, while cosine similarity focuses on the *direction* of the vectors, not their size. That means two documents with the same word distribution but different lengths can still be considered similar.

Cosine similarity measures how aligned the vectors are, which is more useful for comparing document content. When vectors are normalized to unit length, cosine similarity can be computed efficiently with a dot product.","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
"What does it mean if cosine similarity is 1, 0, or -1 between two vectors?","If cosine similarity is:

- **1**: the vectors are **identical** in direction
- **0**: the vectors are **orthogonal** — meaning **no similarity**
- **-1**: the vectors are **completely opposite** in direction","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
"Can cosine similarity be used with TF-IDF vectors, and why does that help in document comparison?","Yes. Cosine similarity can be used with TF-IDF vectors.

TF-IDF vectors are often used in text analysis, and cosine similarity measures the angle between two vectors, so it tells you how similar two documents are based on their content rather than their raw length. This helps because documents with similar word usage will point in a similar direction, even if one document is much longer than the other.","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
"What kind of vectors can cosine similarity compare, and what does the angle between them tell us?","Cosine similarity can compare **non-zero vectors**, such as **word count vectors, TF-IDF vectors, or embeddings**.

The angle between them tells us how similar they are:
- **Smaller angle** = more similar
- **0°** = identical direction, so similarity is highest
- **90°** = orthogonal, meaning no similarity
- **180°** = opposite vectors, meaning completely dissimilar","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
Why can cosine similarity be computed with a matrix multiply once the embeddings are normalized?,"Because once embeddings are normalized to unit length, cosine similarity is just the dot product. That means you can stack all embeddings into a matrix and compute all similarities to the query with one matrix multiplication efficiently.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
"What changes when the vectors are unit length, and why does that make cosine similarity easier to calculate?","When the vectors are **unit length**, their magnitudes are all 1, so the normalization part of cosine similarity disappears.

That means cosine similarity becomes just the **dot product**:

- Normally:  
  \[
  \cos(\theta) = \frac{a \cdot b}{\|a\|\|b\|}
  \]

- If both vectors are normalized to unit length:  
  \[
  \|a\| = \|b\| = 1 \Rightarrow \cos(\theta) = a \cdot b
  \]

This makes it easier to calculate because you no longer need to divide by vector norms for every pair. You can compute similarities efficiently using a **matrix multiplication** between the embeddings and the query vector.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
How does normalizing the document embeddings and query help turn cosine similarity into a dot product?,"When both the document embeddings and the query are normalized to unit length, their magnitudes are 1. That means cosine similarity

\[
\cos(\theta) = \frac{a \cdot b}{\|a\|\|b\|}
\]

simplifies to just the dot product:

\[
\cos(\theta) = a \cdot b
\]

because \(\|a\| = \|b\| = 1\). So normalizing turns cosine similarity into a simple dot product, which can then be computed efficiently with matrix multiplication.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
"Why do we say cosine similarity ignores vector size, and how does that connect to matrix multiplication?","Cosine similarity “ignores vector size” because it measures only the **angle** between vectors, not their length. So two vectors pointing in the same direction are considered very similar even if one is much longer.

This connects to matrix multiplication like this:

- If vectors are **normalized to unit length**, their magnitudes are all 1.
- Then cosine similarity becomes just the **dot product**.
- A dot product between a query vector and many document embeddings can be computed efficiently as a **matrix multiplication**.

So the idea is:

1. Normalize embeddings and query.
2. Compute `X.dot(q)` where `X` is the matrix of document embeddings.
3. Each result is the cosine similarity with the query.

That’s why cosine similarity can be implemented efficiently with matrix multiplication.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
"In vector search, why is it enough to compare normalized embeddings with the query using dot products?","Because if the embeddings are **normalized to unit length**, then the **dot product equals cosine similarity**.

So for a normalized query and normalized document vectors:

- their magnitudes are both 1
- cosine similarity simplifies to just the dot product
- that means you can compute similarity efficiently with a matrix multiplication

If vectors are **not normalized**, then dot products do **not** correspond to cosine similarity, so you’d need to normalize them first.

","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
Why am I getting `JSONDecodeError: Expecting value` when I try to parse the LLM output with `json.loads`?,"Because the LLM output isn’t always pure JSON. It may wrap the JSON in a markdown code fence or add extra text/prose before or after it, so `json.loads(response)` fails with `JSONDecodeError: Expecting value`.

To fix it, force the model to return JSON only:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also make the prompt very explicit, for example:

```text
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
How can I make the model return only valid JSON instead of wrapping it in a code block or extra text?,"Use the provider’s JSON mode if it has one, and make the prompt explicit.

Example with OpenAI:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also tell the model exactly what shape you want, for example:

```text
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

If you use another provider, check for the equivalent setting, such as Gemini’s `response_mime_type=""application/json""` or Groq’s `response_format`.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
What should I add to my prompt so the LLM outputs a JSON object with a `questions` list and nothing else?,"Add this to your prompt:

> Output a JSON object with a single key `""questions""` whose value is a list of 5 strings. Do not include any extra text, explanation, or formatting.

If you can, also force JSON output with the API using `response_format={""type"":""json_object""}`.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
Can I force structured JSON output with OpenAI when generating ground-truth questions for evaluation?,"Yes. You can force structured JSON output with OpenAI by using `response_format={""type"": ""json_object""}`.

Example:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

It also helps to make the prompt explicit about the shape, for example:

```text
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

So yes — this is the recommended way to avoid markdown fences or extra prose.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
Do other LLM APIs like Gemini or Groq have a way to return JSON directly?,"Yes. According to the context, most providers have an equivalent JSON output mode:

- **Gemini**: `response_mime_type=""application/json""`
- **Groq**: `response_format`  
- And other providers may have similar options too.

If you want, I can also show a small example for Gemini or Groq.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
Why does my Jupyter kernel die when I try to embed the ground-truth data in Module 4?,"Your Jupyter kernel is probably dying because the machine runs out of memory when the embedding model is loaded together with the rest of the notebook state.

Common fixes:
- Use a smaller embedder like `sentence-transformers/all-MiniLM-L6-v2` (384-dim).
- Run the embedding step in a separate Python script from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine with more RAM or run locally.
- Process the ground-truth set in batches and free memory between batches with `del` and `gc.collect()`.

","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
What can I do on a low-RAM machine if the notebook crashes while loading the embedding model?,"On a low-RAM machine, you can:

- switch to a smaller embedder, like `sentence-transformers/all-MiniLM-L6-v2`
- move the embedding step into a separate Python script and load the saved vectors back into the notebook
- use a machine with more RAM, or run locally
- process the ground-truth set in batches and free memory between batches with `del` and `gc.collect()`

If you switch models, re-run the evaluation because the metrics will change.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
"Is there a smaller embedding model I can swap in to avoid memory issues, and will that affect my eval results?","Yes — you can swap in a smaller embedder like `sentence-transformers/all-MiniLM-L6-v2` (384-dim) to reduce memory use.

Yes, it will affect your eval results: changing the embedding model will change your hit-rate / MRR numbers, so you should re-run the evaluation after switching.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
Would it help if I move the embedding part out of the notebook and run it as a separate script?,"Yes — that’s a recommended workaround.

The context says you can **move the embedding step into a separate Python script**, run it from the terminal, and then **load the saved vectors back into the notebook**. This helps because the notebook may run out of memory when the embedding model is loaded alongside the rest of the notebook state, especially on small-RAM machines.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
How can I handle the ground-truth embeddings in batches so I don’t run out of memory?,Process the ground-truth set in batches and free memory between batches using `del` and `gc.collect()`.,"Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
Why does adding `time.sleep(4)` still not prevent me from hitting Gemini limits while creating the ground-truth dataset?,"Because `time.sleep(4)` only helps with the **per-minute** limit. Gemini also has a **per-day** request quota, and a long `tqdm` loop can still use up that daily allowance in a single run.

So even if you slow the requests down, you can still hit the daily cap while generating the ground-truth dataset.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
What can I do if the free Gemini quota runs out in the middle of a long `tqdm` loop?,"You can:

- **Resume from where you stopped** by saving partial progress periodically, for example to a **JSONL file**.
- If you want to avoid the limit entirely, **switch to a different model/provider** like **OpenAI `gpt-4o-mini`** or **Groq’s free tier**.
- Also **lower concurrency** if you’re using a thread pool.

A `time.sleep(4)` only helps with **per-minute** limits; it won’t prevent hitting the **per-day** free Gemini quota in a long `tqdm` loop.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
"Is it worth switching to OpenAI `gpt-4o-mini` instead of Gemini for dataset generation, and why?","Yes — if you’re generating the ground-truth dataset, it’s worth considering switching to OpenAI `gpt-4o-mini`.

Why:
- **Cheaper**: the course notes say it’s cheap enough to generate/embed the whole dataset.
- **Higher rate limits**: it’s less likely to hit the per-minute/per-day limits that free-tier Gemini can hit.
- **More reliable for long batch runs**: the context specifically recommends it when Gemini rate limits become a problem.

So the tradeoff is basically:
- **Gemini**: free tier, but easier to hit limits.
- **`gpt-4o-mini`**: small cost, but smoother for dataset generation.

If you want, I can also compare it with **Groq’s free tier** as an alternative.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
"Can I use Groq’s free tier for this task, and which model should I pick?","Yes — you can use Groq’s free tier for development, and it supports tool use.

For the model, the context mentions **Deepseek R1** or **Llama 4** as Groq models with tool-use support. If you want a safe default, pick **Deepseek R1**.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
How should I change my thread pool or save progress so I can continue after a rate-limit error?,"Lower the concurrency and save progress as you go.

Specifically:

- Use a smaller thread pool, around **2–3 workers**, instead of a large pool.
- **Checkpoint partial results periodically** to a file, such as **JSONL**.
- Then, if you hit a rate limit, you can **resume from the saved progress** instead of restarting from scratch.

If you want, I can also show a simple pattern for checkpointing a thread-pool loop.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
How do I fix the WinError 126 message when torch tries to load fbgemm.dll on Windows?,"To fix `OSError: [WinError 126] ... error loading fbgemm.dll` on Windows:

- Install the **Visual C++ Redistributable**
- If that doesn’t help, install **Visual Studio** itself — **not Visual Studio Code**

More details are in the PyTorch discussion here:  
https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
"If I get ""The specified module could not be found"" for fbgemm.dll in Python on Windows, what should I install?","Install the **Visual C++ Redistributable**.

If that doesn’t fix it, also install **Visual Studio** (not Visual Studio Code).","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
Does this torch fbgemm.dll import error usually mean I need Visual C++ Redistributable or Visual Studio on Windows?,"Yes — in this course context, that `torch\lib\fbgemm.dll` import error on Windows is usually fixed by installing the **Visual C++ Redistributable** or **Visual Studio** (not Visual Studio Code).","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
I'm seeing a missing dependency error for fbgemm.dll in PyTorch on Windows 10/11 — what’s the workaround?,"Try one of these workarounds:

1. **Install the Visual C++ Redistributable**
2. **Install Visual Studio**  
   - Important: install **Visual Studio**, **not** Visual Studio Code.

If the issue persists, see the PyTorch discussion here:  
https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
What can I do when Python says it can’t load torch\lib\fbgemm.dll because a module is missing?,"Install the **Visual C++ Redistributable** first. If that doesn’t fix it, install **Visual Studio** (not Visual Studio Code).

For more details, see: https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
"Why am I getting a psycopg2 OperationalError that says it can’t translate host name ""postgres"" when I run prep.py?","You’re getting that error because `prep.py` is trying to connect to a database host named `postgres`, but on your setup that hostname isn’t resolvable.

To fix it, update your `.env` file:

```ini
POSTGRES_HOST=localhost
```

Then run `prep.py` again.","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
"How do I fix the ""could not translate host name postgres to address"" error in prep.py?","Update your `.env` file and set:

```ini
POSTGRES_HOST=localhost
```

This fixes the `could not translate host name ""postgres"" to address` error in `prep.py`.","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
What should I change in my .env file if postgres host is not found when running the monitoring prep script?,"Change `POSTGRES_HOST` in your `.env` file from `postgres` to `localhost`:

```ini
POSTGRES_HOST=localhost
```","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
Is there a reason I need to set POSTGRES_HOST to localhost instead of postgres for this error?,"Yes — based on the error, `postgres` is not resolving as a hostname from where `prep.py` is running. Setting `POSTGRES_HOST=localhost` tells your script to connect to PostgreSQL on your local machine instead.

So the reason is:  
- `postgres` is typically the Docker service/container name.
- `localhost` is the host machine itself.

If you are running `prep.py` outside Docker, `localhost` is the correct setting.","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
My python prep.py crashes with OperationalError about postgres host resolution — what’s the correct env setting to use?,"Set `POSTGRES_HOST=localhost` in your `.env` file.

```ini
POSTGRES_HOST=localhost
```","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
How do I make Pandas show the full text in a column instead of cutting it off?,"Use:

```python
pd.set_option('display.max_colwidth', None)
```

This tells Pandas not to truncate the text in the column.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
Is there a setting to stop Pandas from truncating long strings in DataFrame columns?,"Yes. Use:

```python
pd.set_option('display.max_colwidth', None)
```

This stops Pandas from truncating long text in DataFrame columns and shows the full content.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
"In the offline-rag-evaluation notebook, how can I see the whole Explanation text from the judge LLM?","Use Pandas’ display option to disable column truncation:

```python
pd.set_option('display.max_colwidth', None)
```

This will let you see the full `Explanation` text from the judge LLM in the offline-rag-evaluation notebook.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
"Why does Pandas only display part of my column text, and how can I change that?","Pandas truncates column text by default so the display stays compact. The default max column width is **50 characters**, and longer text is shown with `...`.

To show the full text, use:

```python
pd.set_option('display.max_colwidth', None)
```

This allows unlimited column width in the displayed output.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
"What option do I need to set so a column with long text shows everything, not just 50 characters?","Set:

```python
pd.set_option('display.max_colwidth', None)
```

This removes the 50-character truncation and shows the full text.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
How can I normalize a vector stored in a Pandas DataFrame column?,"You can normalize the vector in the DataFrame column with `numpy.linalg.norm` and `apply`:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```

This creates a new column with normalized vectors.","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
What’s the easiest way to make every vector in a Pandas Series have length 1?,"Use normalization:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)
series = series.apply(normalize_vec)
```

This makes every vector have length 1.","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
Is there a simple Pandas apply example for normalizing vectors in one column?,"Yes — a simple Pandas `apply` example is:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
How do I divide each vector by its own norm inside a DataFrame column?,"To divide each vector by its own norm inside a DataFrame column, you can apply a normalization function to the column:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```

This divides each vector by its own norm.","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
Can I normalize a whole column of numpy vectors with a lambda function in Pandas?,"Yes. You can use a lambda with `apply` to normalize each numpy vector in a Pandas column:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)
df[""new_col""] = df[""org_col""].apply(normalize_vec)
```

This normalizes each vector in the column.","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
How do I get the 75th percentile for a column in a Pandas DataFrame?,"Use the `quantile` method with `q=0.75`:

```python
quantile = df[""col""].quantile(q=0.75)
```

","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
What’s the Pandas way to calculate a 0.75 quantile on a Series?,"Use:

```python
series.quantile(q=0.75)
```

For example:

```python
quantile = df[""col""].quantile(q=0.75)
```","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
How can I find a column’s percentile value in Pandas without using numpy?,"Use Pandas’ built-in `quantile()` method:

```python
percentile_value = df[""col""].quantile(q=0.75)
```

For example, `q=0.75` gives the 75th percentile.","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
Is there a simple method in Pandas to compute a quantile for one column?,"Yes. You can use the `quantile()` method on a Pandas column or Series:

```python
quantile = df[""col""].quantile(q=0.75)
```

This computes the 75th percentile (0.75 quantile).","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
How do I extract the 75% cutoff from a DataFrame column using Pandas?,"Use `quantile(q=0.75)` on the column:

```python
cutoff = df[""col""].quantile(q=0.75)
```

This gives the 75% cutoff.","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
"How do I wipe out every Docker container from the terminal, even the ones still running?","Use:

```bash
docker rm -f $(docker ps -aq)
```

This deletes all containers, including the running ones.","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
What’s the command to force-remove all Docker images at once?,"Use:

```bash
docker rmi -f $(docker images -q)
```

This force-removes all Docker images at once.","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
How can I delete every Docker volume using the command line?,"Use:

```bash
docker volume rm $(docker volume ls -q)
```

This deletes all Docker volumes from the command line.","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
"Is there a quick way to clean up all containers, images, and volumes in Docker without doing them one by one?","Yes. You can clean up all containers, images, and volumes from the terminal with these commands:

```bash
docker rm -f $(docker ps -aq)
docker rmi -f $(docker images -q)
docker volume rm $(docker volume ls -q)
```

These remove all containers, all images, and all volumes, respectively.","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
"What terminal commands should I run to fully clear Docker stuff like containers, images, and volumes?","Run these commands:

```bash
docker rm -f $(docker ps -aq)
docker rmi -f $(docker images -q)
docker volume rm $(docker volume ls -q)
```

These will delete all containers, images, and volumes.","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
"How can I make sure a user can only send feedback once per submission in Streamlit, like just +1 or -1?","Use `st.session_state` to track whether feedback has already been given for the current submission, and only render/enable the +1 / -1 buttons if that flag is still `False`.

In the context, the issue is specifically that `st.session.submitted` is being reassigned to `True` again after one feedback button is pressed. The referenced solution is on Streamlit Discuss:

https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2

So the short answer is: keep a session-state flag for the current submission, and prevent it from being reset after a feedback click. If you want, I can help you rewrite the Streamlit code pattern for this.","Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
Why does `st.session.submitted` keep getting set back to `True` even after I click one of the feedback buttons?,"It’s being reassigned somewhere else in the app logic during reruns. The course note says this was solved, and points to the Streamlit Discuss solution:

https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2","Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
"After I press the ask button, what’s the right way to disable the buttons until the submission state changes?",I don't know.,"Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
Is there a known Streamlit session-state issue where a session flag gets reassigned unexpectedly after a button click?,"Yes — this is a known Streamlit session-state issue. In the provided context, the problem with `st.session.submitted` being reassigned to `True` after a feedback button click is explicitly mentioned, and the solution is linked from Streamlit Discuss.

","Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
Where can I find the fix for the problem where `st.session.submitted` changes again after feedback is given?,"The fix is in the Streamlit Discuss solution linked here:

https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2","Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
"Why does my streamlit app fail in docker-compose with ""streamlit: executable file not found in $PATH"", even though it works locally?","Because `docker-compose` is trying to run `streamlit` inside the container, but the `streamlit` executable isn’t available in that container’s PATH.

According to the context, the fix is to:

1. Make sure you have a `Dockerfile`.
2. Add `streamlit` properly in the `docker-compose` setup.
3. Rebuild the container:

```bash
docker-compose up --build
```

It works locally because your local environment already has Streamlit installed, but the Docker container is a separate environment and needs Streamlit installed/configured there too.","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
"Do I need a Dockerfile when running a streamlit app with docker-compose, or is compose config enough?","Yes — based on the context, you should **create a Dockerfile**. The compose config alone is not enough here.

The issue described was fixed by:

1. Creating a `Dockerfile`
2. Adding `streamlit` to the `docker-compose` configuration
3. Rebuilding with:

```bash
docker-compose up --build
```

So the answer is: **you need a Dockerfile, not just docker-compose config**.","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
How do I fix the OCI runtime error when docker-compose says it can't find the streamlit command?,"To fix the OCI runtime error in `docker-compose`:

1. Make sure you have a `Dockerfile`.
2. Add `streamlit` to your `docker-compose` configuration.
3. Rebuild and start the app:

```bash
docker-compose up --build
```

This should resolve the issue where Docker can’t find the `streamlit` executable in the container’s `$PATH`.","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
What should I change in docker-compose so my streamlit container starts correctly?,"Add `streamlit` to your `docker-compose` service command/config and make sure you have a `Dockerfile` for the app, then rebuild with:

```bash
docker-compose up --build
```

If you want, I can help you rewrite your `docker-compose.yml` too.","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
"After adding streamlit to docker-compose, do I need to rebuild the image or just restart the containers?","You should rebuild the image, not just restart the containers.

The context says to run:

```bash
docker-compose up --build
```

So after adding `streamlit` to `docker-compose`, use `--build` to recreate the image with the updated config.","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
"Is the capstone project something we do alone, or can it be a group project?","The capstone project is **individual**, not a group project.

You can collaborate or discuss ideas with others, but **each submitted project must stand on its own** and be independently evaluable.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
"Do students work in teams for the capstone, or is it individual only?","The capstone is **individual only**. Students can discuss ideas or collaborate informally, but each submitted project must be independently evaluable and have its own repository.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
"Can I team up with someone for the capstone, or do I have to do it solo?","You have to do the capstone individually — it’s an individual attempt, not a team project.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
Is the final capstone meant to be completed by one person or by a group?,"The capstone is meant to be completed by **one person**, not a group. It is an **individual project**.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
"For the capstone, do we need partners or is it a solo assignment?","No— the capstone is an individual project, not a group assignment. You can discuss ideas with others, but each submitted project must stand on its own.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
"Do I have to submit two separate capstone projects, or is one project enough for the course?","One project is enough. You only need to submit one capstone project.

If your first submission doesn’t pass, you can improve it and resubmit in attempt #2. You can only submit a second project if you want extra experience, and then it must use a different dataset and problem statement.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
What exactly do attempt 1 and attempt 2 mean for the capstone submission timeline?,"Attempt 1 and attempt 2 are two submission windows for the **same capstone project**:

- **Attempt 1**: your first chance to submit the project.
- **Attempt 2**: a later resubmission window if you need to improve the project after attempt 1, or if you missed the first window.

You **do not need to submit two different projects**. You submit **one project**, and if the first submission doesn’t pass, you can fix it and submit again in attempt 2.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"If my first capstone submission doesn’t pass, can I fix it and send it again later?","Yes. If your first capstone submission doesn’t pass, you can improve it and resubmit during the attempt #2 submission window.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"Can I submit two projects just to get more practice, and if so do they need to be on different topics?","Yes — you can submit two projects for more practice, but they need to use different datasets and problem statements.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"If I miss the first submission window, do I still have a chance to submit in the second one and stay eligible?","Yes. If you miss the first submission window, you can still submit in the second attempt window and catch up.

Just make sure you also participate in the peer-review of three peers in your cohort, because the submission only counts toward certification if you do that.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"Is the competition itself accepted as the capstone project, or is the capstone supposed to be something else?","No. The competition itself is **not** accepted as the capstone project. The capstone is supposed to be an **individual project**. You may join the competition as a group if you want, but that does **not** count as the capstone.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
Can I join the math-kaggle LLM competition with a team and still use that for my capstone?,"Yes, you can join the math-kaggle-llm-competition as a group, but the capstone itself is an individual attempt. So you can participate in the competition with a team, but that team participation does not count as your capstone.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
"If I want to form a group for the competition, does that replace the individual capstone requirement?","No. The competition can be done as a group, but the capstone requirement is still individual and does not get replaced.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
"Does taking part in the competition count toward the capstone, or do I still need to do an individual project?","The competition does **not** count toward the capstone. You still need to do an **individual capstone project**.

You can participate in the competition as a group if you want, but the capstone itself must be your own individual attempt.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
Can the capstone be done as a team if I'm already participating in the competition?,"No. The capstone is an individual attempt, even if you participate in the competition as a team.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
How will my capstone project be graded?,"Your capstone project will be graded by **three randomly assigned students** who also submitted the project. You will also grade **three fellow students’ projects** yourself.

Your **final grade** will be the **median score** of the peer reviewers’ grades.","Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
Who actually reviews the capstone submissions?,The capstone submissions are reviewed by **three randomly assigned students who have also submitted the project**. You also have to grade **three fellow students’ projects** yourself.,"Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
Do I need to evaluate other students' projects too?,"Yes — to get the certification, you must participate in peer review and evaluate **three peers** in your cohort.","Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
What happens if I skip the peer-review part?,"If you skip the peer-review part, your submission does **not count toward certification**. To get the certificate, you must complete the capstone project **and** review three peers in your cohort.","Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
How is the final score for the capstone calculated?,The final capstone grade is the **median score** of the grades given by the **three peer reviewers** who evaluate your project.,"Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
"When do we get the projects to review and grade, and how are they assigned?","After the submission deadline has passed, an Excel sheet will be shared, and each participant will be assigned 3 projects to review and grade. You can’t choose the projects yourself.","After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
Will project review assignments come before or after the submission deadline?,After the submission deadline has passed.,"After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
How many projects does each participant have to grade in the capstone project?,Each participant has to grade **3 projects** from fellow students.,"After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
What happens once the submission deadline is over for project grading?,"After the submission deadline passes, an Excel sheet is shared and each participant is assigned 3 projects to evaluate.","After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
"Do we get an Excel sheet with the review assignments, and what will it include?","Yes. After the submission deadline has passed, an Excel sheet will be shared with the review assignments, and it will include 3 projects assigned to each participant.","After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
"I already turned in my capstone project, so why am I not seeing anything to review yet?","Once the project submission deadline has passed, projects are assigned for evaluation. You can’t choose which projects to review, and you can’t review anything before the list is released.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
When do the projects for evaluation get assigned after the submission deadline ends?,"After the submission deadline has passed, an Excel sheet is shared and 3 projects are assigned to each participant.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
"Can I pick which capstone projects I want to review, or is that decided for me?","You can’t choose them yourself. After the project submission deadline passes, the projects are assigned to you for evaluation, and you can’t review before the list is released.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
Is there any way to start reviewing submissions before the project list is released?,No. You can’t review before the project list has been released.,"Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
"I submitted my project already, but I still can't review others — is that normal?","Yes, that’s normal.

After the project submission deadline has passed, projects are assigned to you for review. You can’t choose which ones to evaluate, and you can’t review before the list is released.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
Where can I look for project ideas and datasets for the capstone project?,Please check the GitHub page for ideas and datasets: https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md,"Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
Is there a place with a few dataset options and example ideas for the LLM Zoomcamp project?,"Yes — the course points you to the **capstone project GitHub page** for that.

You can find several **idea and dataset options** here:
[project.md](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md)

That page includes project ideas, datasets, and tips/guidelines for the LLM Zoomcamp project.","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
What’s the best link to check for capstone project inspiration and data sources?,"The best link is this GitHub page:

https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md

It includes ideas, datasets, tips, and guidelines for the capstone project.","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
"Can you point me to a GitHub page that lists project ideas, datasets, and some guidance?","Yes — check this GitHub page: https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md

It includes several project ideas, datasets, and tips/guidelines.","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
I’m stuck on picking a project — where can I find suggestions and usable datasets?,"Please check this GitHub page for several project ideas and usable datasets, along with tips and guidelines:

https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
"Do I have to use Airflow, Mage, or Kestra for the capstone project, or is a simple Python script enough?","No — you do **not** have to use Airflow, Mage, or Kestra for the capstone project.

A **plain Python script** that ingests and indexes your data is enough for full points on the **ingestion pipeline** criterion. A Jupyter notebook with the same steps would also work, but with fewer points.

Use an orchestrator only if it genuinely fits your project, such as for recurring daily ingestion.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
"If I build the ingestion and indexing steps in a Jupyter notebook, will that count for the full pipeline points?",No. A Jupyter notebook with the ingestion and indexing steps is worth **1 point instead of 2** for the ingestion-pipeline criterion. A plain Python script is enough for full points.,"No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
When does it actually make sense to add an orchestrator to a capstone project instead of just using Python?,"Use an orchestrator only when it actually fits the project, not just to satisfy the capstone requirement.

According to the context, a plain Python script that ingests and indexes your data is enough for full points on the ingestion pipeline criterion. An orchestrator like Airflow, Mage, or Kestra makes sense only for cases like recurring ingestion of a feed that updates daily.

So the short answer is: add one only if your project genuinely needs scheduled or repeated workflow orchestration. Otherwise, just use Python.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
Can I still get the ingestion pipeline criterion without setting up a workflow tool at all?,"Yes. You can still get full points for the ingestion pipeline criterion without setting up any workflow/orchestration tool.

A plain Python script that ingests and indexes your data is enough. A Jupyter notebook with the same steps is also acceptable, but it’s worth 1 point instead of 2.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
"Should I avoid adding an orchestration tool just for grading, and only use one if my data gets updated regularly?","Yes — for the capstone, you should **not add an orchestration tool just to get points**. A **plain Python script** that ingests and indexes your data is enough for full points on the ingestion pipeline criterion.

Use an orchestrator only if it **actually fits your project**, for example if your data is **updated regularly** and you need recurring ingestion.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
How can I evaluate a recommender-style RAG project if I don't have clear ground-truth answers?,"Use two complementary approaches:

1. **Synthetic ground truth**
   - For each item in your dataset, ask an LLM to generate about 5 user queries that should retrieve that item as the top result.
   - Run those queries through your retriever.
   - Measure **hit rate**, **MRR**, or **NDCG**.

2. **LLM-as-a-judge**
   - Sample real or synthetic queries.
   - Run the full RAG/recommender pipeline.
   - Have an LLM score the result with a fixed rubric, such as **relevance** or **usefulness** on a **1–5 scale**.

For ranking-style recommender problems, **NDCG** is often a better fit than hit rate because it rewards getting good items higher in the list, not just the first result.

","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
Is there a good way to create test queries for items in my dataset so I can measure retrieval quality?,"Yes — a good approach is to create **synthetic ground truth**.

For each item in your dataset, prompt an LLM with that item’s description and ask it to generate about **5 user queries** that should return that item as the **top result**. Then run those queries through your retrieval system and measure metrics like:

- **hit rate**
- **MRR**
- **NDCG**

For ranking-style problems, **NDCG is often a better fit than hit rate**, because it rewards putting good items higher in the list, not just getting the first match.

","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
"What metrics should I use for a ranking-style RAG system: hit rate, MRR, or NDCG?","For a ranking-style RAG system, use **all three if you can**, but they tell you different things:

- **Hit Rate**: tells you whether at least one relevant item appears in the top-k.
- **MRR**: tells you how high the first relevant item appears.
- **NDCG**: is often the **best fit for ranking-style problems**, especially when **multiple items can be acceptable**.

From the context: **NDCG is often a better fit than hit rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.**

So the practical recommendation is:

- Use **Hit Rate** as a simple coarse success measure.
- Use **MRR** if the position of the first relevant result matters.
- Use **NDCG** if you care about the overall ranking quality and graded relevance.

If you want one primary metric for a ranking-style RAG system, **NDCG** is usually the best choice.","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
How do I use an LLM as a judge to score the output of my RAG pipeline end to end?,"Use an **LLM-as-a-judge** in an end-to-end evaluation loop:

1. **Sample queries** from your target use case.
2. **Run the full RAG pipeline** on each query.
3. **Ask another LLM to grade the result** against a fixed rubric you define in the prompt, for example:
   - relevance to the query
   - usefulness/completeness
   - correctness
   - overall quality  
   Use a **1–5 scale** or similar.

A good pattern is to have the judge LLM return both:
- a **numeric score**, and
- a short **explanation** for the score.

If you’re inspecting the judge outputs in Pandas, you can show the full explanation text with:

```python
pd.set_option('display.max_colwidth', None)
```

If you want, I can also help you draft a judge prompt/rubric for your specific RAG pipeline.","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
"For recommenders where several items could be acceptable, why would NDCG be better than just checking whether the top result is correct?","Because NDCG doesn’t just ask “was the first result correct?” It rewards ranking the acceptable items higher overall.

If several items are acceptable, a recommender can still be good even when the top result isn’t the only right one. NDCG captures that by giving more credit to relevant items near the top of the list, while hit rate only checks whether there was at least one correct result in the top-k.","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
How should I chunk a big PDF corpus without overthinking it at first?,"Start simple: extract the PDF text, split it into chunks, and keep related context together. Don’t over-optimize the chunking scheme on day one.

A good first pass is to:
- remove obvious noise like repeated headers/footers, OCR garbage, boilerplate, and duplicates
- preserve useful structure like titles, section names, page numbers, dates, and Q&A pairs
- store the text in a structured format that’s easy to process
- chunk the documents so each chunk contains coherent related content
- keep metadata for filtering or ranking later

The main goal is not just making the text shorter, but making retrieval more accurate.

If you want, I can also suggest a very practical “first chunking recipe” for PDFs.","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
Is it better to use fixed-size chunks or ask an LLM to split docs into sections?,"It’s usually best to **start with fixed-size chunks** and evaluate first.

From the context:
- **Start simple:** fixed-size chunking, about **1000 tokens with some overlap**.
- Then **try smart chunking**: ask an LLM to split documents into logical sections and index those sections.

So the recommended approach is **iterate**, not to assume LLM-based splitting is always better. Run a small ground-truth eval after each change and compare hit rate / MRR.","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
"Should I add summaries to each chunk before indexing, and does that actually help retrieval?",I don't know.,"Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
"For long legal or financial docs, is BM25 plus dense search better than only embeddings?","Yes — for long, structured legal or financial documents, the context recommends **hybrid search (BM25 + dense embeddings)** rather than using only embeddings.

Reason: exact wording matters in those docs, and BM25 helps preserve matches on specific terms while dense search handles semantic similarity.

","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
What are good tools to turn PDFs into cleaner markdown before I chunk them?,"Good options from the course context are:

- **`pymupdf4llm`** — fast and gives decent Markdown output.
- **Docling** — slower, but usually higher quality, especially for **tables and figures**.
- **GROBID** — best suited for **academic papers**, since it extracts structure like sections and references.
- Also mentioned for broader PDF/text extraction:
  - **PyMuPDF**
  - **unstructured**
  - **pytesseract** for scanned/image-only PDFs
  - **hosted OCR** like **Mistral OCR**

If you want a practical starting point, try **`pymupdf4llm`** first, then compare it with **Docling** on a sample of your PDFs.","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
What does reproducibility mean for the project review?,"Reproducibility means a peer reviewer can clone your repo and follow your README to rebuild and run the system from scratch using their own credentials.

So:
- Do **not** include API keys or hosted-service credentials in the repo.
- Provide a script or notebook to ingest the dataset and rebuild the search index locally.
- Include a `.env.example` with variable names only, and keep `.env` out of git.
- Pin dependency versions and document the Python version, and Docker version if used.","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
Do I need to put my API keys or service credentials in the repo so the reviewer can run it?,"No. Never put your API keys or hosted-service credentials in the repo.

For reproducibility, the reviewer should be able to:

- clone your repo,
- follow your README,
- rebuild everything from scratch,
- and use their own credentials.

What to include instead:

- a script or notebook that rebuilds the index/data locally,
- a `.env.example` with variable names only, no secrets,
- `.env` added to `.gitignore`,
- pinned dependency versions and the Python version you used.

If you want, I can also help you draft a good `.env.example` for your project.","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
How should I let someone rebuild the search index from the dataset on their own machine?,"Provide a script or notebook that ingests the dataset and rebuilds the search index locally on their machine. Also include a `.env.example` with only variable names, and have them create their own `.env` with their own credentials.","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
"What should go into a .env.example file, and should .env be committed?","A `.env.example` file should contain the **variable names only**, with **placeholder values or no values**, so reviewers know what to put in their own `.env`.

Example:

```env
OPENAI_API_KEY=
GROQ_API_KEY=
GEMINI_API_KEY=
```

You should **not commit your real `.env` file**. Add `.env` to `.gitignore` and keep your actual keys local.

So:

- **`.env.example`**: yes, commit this
- **`.env`**: no, do not commit this","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
"How do I make the project easier and cheaper for reviewers to run, like with model choice and pinned versions?","To make it easier and cheaper for reviewers to run your project:

- Use a **cheap model** for demos and testing, like **`gpt-4o-mini`** or **Groq**, so reviewers don’t spend much.
- **Pin dependency versions** in `requirements.txt` or a lock file in `pyproject.toml`.
- **Document the Python version** you used, and Docker version too if you use Docker.
- Provide a script or notebook that **rebuilds the search index locally** from the dataset.
- Include a **`.env.example`** with variable names only, and keep real `.env` files out of git.

Also, since the course is Python-based, **Python is the easiest and most reviewer-friendly choice**.","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
"Can I reuse the workshop content in my own capstone project, or is it only for the class?","Yes — you can reuse the workshop content in your own capstone project. Since dlt is open-source, the workshop material can be used directly for a capstone or other projects.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
"Since dlt is open-source, am I allowed to use these materials in another project outside the workshop?","Yes. Since dlt is open-source, you can use the workshop materials in other projects outside the workshop, including your own capstone or other Zoomcamp projects. Feel free to use and share them.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
"Could I apply what we learned here to a different Zoomcamp project, like the MLOps one?","Yes — the workshop materials can be used for other Zoomcamps, including the MLOps Zoomcamp project. Since dlt is open-source, you’re allowed to use it in your own projects.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
"Is it okay if I share the workshop resources with other people, or are they meant just for students?","Yes — the workshop materials are open-source, so you can use them in your own projects and share them with others. The goal is not to keep them only for students.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
"If I want to use dlt directly in my own project, do I need any special permission, or can I just go ahead and do it?","You can just go ahead and use dlt directly in your own project — no special permission is needed. Since dlt is open-source, the course says you’re free to use it in your projects.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
How do I start a new dlt project for loading data from cloud storage?,"Start with this command on the command line:

```bash
dlt init filesystem duckdb
```

You can also find more guidance at: https://dlthub.com/docs/tutorial/filesystem","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
What's the command to initialize a dlt pipeline with filesystem and DuckDB?,`dlt init filesystem duckdb`,"Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
"If I want to ingest cloud files into DuckDB with dlt, what should I run first?","Run:

```bash
dlt init filesystem duckdb
```

","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
How can I create a fresh dlt setup for a filesystem source?,"Use the filesystem init command:

```bash
dlt init filesystem duckdb
```

This creates a fresh dlt setup for a filesystem source.","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
Is there a quick init command for a dlt project using filesystem plus DuckDB?,"Yes — use:

```bash
dlt init filesystem duckdb
```","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
"Why do I get `Table notion_pages___homework does not exist` when I call `db.open_table(""notion_pages___homework"")`?",Because the pipeline/config still contains the old table name `employee_handbook` in some places. You need to replace **all** instances of `employee_handbook` with `homework` so the table gets created with the name you’re trying to open.,"The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
"I changed the table name in one place, but now opening `notion_pages___homework` still fails. What am I missing?","You’re probably still missing another occurrence of the old table name in your pipeline settings.

The context says the error happens when you haven’t changed **all instances of `""employee_handbook""` to `""homework""`**.

So check for any leftover references to the old name, not just the one table open call.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
"What could cause `db.open_table(""notion_pages___homework"")` to say the table doesn't exist even though my pipeline ran?","A likely cause is that your pipeline still has some references to `employee_handbook` instead of `homework` in the settings.  

The context says this exact error can happen when you haven’t changed all instances of `""employee_handbook""` to `""homework""` in your pipeline configuration.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
Is there another setting besides the table name I need to update when switching from `employee_handbook` to `homework`?,"Yes. You likely need to update all occurrences of `employee_handbook` in your pipeline settings, not just the table name.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
How do I fix the `FileNotFoundError` for `notion_pages___homework` in the dlt workshop?,"The fix is to make sure you changed all instances of `""employee_handbook""` to `""homework""` in your pipeline settings.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
Why am I getting FileNotFoundError: Table notion_pages___homework does not exist when I run main() in the dlt workshop?,"The error usually means your pipeline/table names weren’t fully updated for the homework. Make sure you changed all occurrences of `employee_handbook` to `homework` in your pipeline settings.

Also, in line 3, open the correct table:

```python
dbtable = db.open_table(""notion_pages___homework"")
```

If that’s already set and you still get the error, I don’t know.","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
"How do I fix the error saying I should first call db.create_table(notion_pages___homework, data) in the Notion pages homework code?","Make sure you changed **all** instances of `""employee_handbook""` to `""homework""` in your pipeline settings. That error usually means the table name is still set incorrectly somewhere.

Also, if needed, verify the table name with:

```python
db.table_names()
```

And the correct line should be:

```python
dbtable = db.open_table(""notion_pages___homework"")
```","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
What table name should I open in line 3 if my script says notion_pages___homework doesn't exist?,"Open this table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```

If it still says the table does not exist, the context says you likely need to replace any remaining `""employee_handbook""` references with `""homework""` in your pipeline settings.","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
"Is db.open_table supposed to use notion_pages___homework in this exercise, and where exactly should I change it?","Yes — in this exercise `db.open_table` should use:

```python
db.open_table(""notion_pages___homework"")
```

You should change it in **line 3** of your code, where the table is opened.  
If you’re still getting the error, make sure you also changed **all** occurrences of `""employee_handbook""` to `""homework""` in your pipeline settings.","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
I keep seeing Table notion_pages___homework does not exist — what’s the correct table to open in main()?,"Open this table in `main()`:

```python
dbtable = db.open_table(""notion_pages___homework"")
```

If you still get the error, the pipeline settings likely still contain some `""employee_handbook""` references that need to be changed to `""homework""`.","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
How can I list all tables that are currently in the database?,You can use the `db.table_names()` method to list all the tables currently in the database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
Is there a way to see the table names from the db object?,"Yes — you can use:

```python
db.table_names()
```

to list all the tables in the database.",You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
What method should I use to check which tables exist in my database?,Use the `db.table_names()` method to list all the tables in your database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
How do I find out what tables were created in the db?,You can use the `db.table_names()` method to list all the tables in the database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
Can I get a list of all tables with db.table_names()?,Yes — you can use `db.table_names()` to list all the tables in the database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
"Does dlt currently support connectors for ClickHouse or StarRocks, or is that still missing?","Currently, dlt does **not** have connectors for **ClickHouse** or **StarRocks** yet. It’s still missing, but the project is open to community contributions to add them.","Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
Can I use dlt to load data into ClickHouse or StarRocks right now?,I don't know.,"Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
Are there official dlt connectors available for ClickHouse and StarRocks?,"No. According to the workshop context, dlt currently does **not** have official connectors for **ClickHouse** or **StarRocks**. It is, however, open to community contributions to add them.","Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
"If dlt doesn't support ClickHouse or StarRocks yet, is the project open to community-built connectors?","Yes. The project is open to community contributions, so even though dlt doesn’t currently have ClickHouse or StarRocks connectors, community-built connectors are welcome.","Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
"Has anyone added ClickHouse or StarRocks connectors to dlt, or are those still not included?","Currently, dlt does **not** have connectors for **ClickHouse** or **StarRocks**. It’s open to community contributions to add them.","Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
"Why am I getting a 401 Unauthorized from the Notion API in the notebook, and what should I check first?","You’re likely getting a 401 Unauthorized because of an authentication problem with the Notion API.

What to check first:
1. **Your API key/token** — make sure it’s correct and has the right permissions.
2. **The Notion API endpoint** — confirm you’re calling the right URL.
3. **Whether the token expired** — regenerate it if needed.
4. **Your notebook/app configuration** — verify all access settings are loaded properly.

If you’re working in a notebook, also make sure any required secrets are actually available there.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
My notebook says it has no secret access to api.notion.com/v1/search — is this an API key problem?,"Yes — this sounds like an authorization / API key issue with Notion, not a notebook problem.

The error in the context says:

- **“Notebook does not have secret access or 401 Client Error: Unauthorized for url: api.notion.com/v1/search”**
- That typically means the **Notion API key / integration token is missing, wrong, expired, or lacks permissions**.

What to check:
1. Make sure you’re using the **correct Notion API key**.
2. Confirm the key has the **needed permissions** for the workspace/page.
3. If the token may have expired or been revoked, **regenerate it**.
4. Verify your notebook or environment is actually loading the secret correctly.

If you want, I can help you check how to set the secret in your notebook environment.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
How do I fix a Notion API authorization error in the dlt workshop notebook?,"If you get a Notion API authorization error in the dlt workshop notebook, it usually means there’s a problem with your Notion credentials or permissions.

Try these fixes:

1. Check that you’re using the correct API key/token.
2. Make sure the token has the right permissions for the Notion API.
3. Verify that the notebook is pointing to the correct Notion API endpoint.
4. If the token may have expired, regenerate it.
5. Double-check all access/configuration settings.

If it still fails, review the Notion API docs and confirm the authentication steps are set up correctly.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
Could the Notion token have expired if I get Unauthorized when calling the search endpoint?,"Yes — the context says a `401 Unauthorized` can indicate an authorization issue, and one thing to check is whether the token has expired and needs to be regenerated.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
What settings or permissions do I need to verify when the notebook can’t access Notion and returns 401?,"Verify these Notion access settings/permissions:

- **API key / secret**: make sure the notebook is using the correct secret or token.
- **Permissions on the token**: the token must have the appropriate access rights for the Notion API.
- **Correct API endpoint**: confirm it’s calling the right Notion endpoint.
- **Token expiry**: check whether the token has expired and regenerate it if needed.
- **All access configurations**: double-check your application’s auth/config settings.

If it still returns 401, it’s an authorization issue and you should review the Notion API authentication setup.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
"Why do I get a 401 Client Error when using the API key, and how do I tell if it's just not granted access yet?","A 401 Client Error usually means the API key is either wrong, missing the needed permissions, or hasn’t been granted access yet.

How to tell:
- If the key is valid but not yet granted access, you’ll still get 401 until the integration/access is approved.
- Double-check that you’re using the correct key and that the app has the right permissions.
- If it persists, verify the endpoint and auth setup.

If you want, I can help you check whether it’s an access-grant issue or a bad key.","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
What's the easiest way to force-install requests 2.32.3 instead of the older 2.28 version for lancedb?,"Use this command to install the correct `requests` version directly from the source:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
Can I install requests straight from the GitHub release zip to match the version lancedb needs?,"Yes — you can install `requests` directly from the GitHub release zip using:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```

This will install the version needed by `lancedb`.","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
"If pip keeps pulling requests v2.28, what exact install command should I run to get v2.32.3?","Run:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
How do I fix the requests version mismatch when setting up the dlt workshop environment?,"To fix the requests version mismatch, install the required version directly from the source:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
